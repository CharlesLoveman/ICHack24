# Database

## Running the Database

1. Install `mongod` from the MongoDB website i.e. here: https://www.mongodb.com/try/download/community
2. Designate some location `DB_PATH` on your computer for the database
3. Run the database

```
mongod --dbpath=DB_PATH
```
